# Application
spring.application.name=Temple Puja Reminder System

# Server
server.port=${PORT:8080}

# Supabase PostgreSQL Database
spring.datasource.url=jdbc:postgresql://db.bdouptwdijegketaxzmd.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=c4831WoeGyFVwu8R
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Thymeleaf
spring.thymeleaf.cache=true
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.mode=HTML

# Logging
logging.level.com.temple.reminder=INFO